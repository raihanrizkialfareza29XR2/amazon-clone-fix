# Installation Steps

## Menggunakan npm

jalankan perintah

1. `npm install`

2. `npm run dev`

## Atau menggunakan yarn

jalankan perintah

1. `npm install --global yarn`

2. `yarn install`

3. `yarn run dev`
